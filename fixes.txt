JS:

- Неиспольземые переменные categoryList, productCards.
- Модалка не закрывается на кнопку escape на клавиатуре.
- Нет смысла в листенере window.onload - та и в принципе в классе modal--init-hidden.
- В функции handleModalClose тернарка усложняет читабельность. Простым ифом ридабилити была бы лучше.
- updateActiveCategory и filterProductCards - используют разные подходы:
	Первая функция снимает со старого активного элемента класс и добавляет новому, а вторая проходится по всем элементам.
	Архитектура первого подхода более простая и предпочтительная, лучше использовать такой подход и в filterProductCards.